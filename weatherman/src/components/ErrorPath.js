import React from 'react';

import { Wrapper, Text } from '../elements';

const ErrorPath = () => (
  <Wrapper>
    <Text>Apologies, this link does not exist...</Text>
  </Wrapper>
);

export default ErrorPath;
